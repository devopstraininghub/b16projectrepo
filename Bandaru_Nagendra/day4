
Day 4 git commands

concept: Branching and mergeing

to check existed or available branches

cmd: git branch

crete new branch

cmd: git branch "branch name"

ex: git branch nagendra

to checkout current branch

cmd: git checkout "current branch name"

ex: git checkout nagendra

incase branch or sub-branch is not available use below cmd. 

cmd: git checkout -b "branch name"

ex: git checkout -b nagendra1

checkout master / main branch 

In local repo use master and central repo use main

ex: git checkout main

ex: git checkout master

for deletaion of git existed or newly created branches, use below cmd's.

cmd: git branch -d "branch name" -->  small "d" for normal deletion.

cmd: git branch -D "branch name" --> it is for forcefully delation of selected branch.


