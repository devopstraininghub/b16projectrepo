Hi all, 
here a small info about Docker. 

Docker is a containarization tool, which is used for creating a containers inside the virtual machine. 

Docker container is a very minimal in size and light weighted, very faster.

$$$$$few commands$$$$$

1. sudo docker ps  ---> to check the containers
2. sudo docker ps -a   -----> shows the stopped containers
3. sudo docker image ls
4. sudo docker image ls -a
5. sudo docker images
6. sudo docker run
7. sudo docker rm
8. sudo docker rmi
